You are an expert full-stack developer specializing in AI automation tools. Build a complete, production-ready Python application that automates job applications for a software development student graduating Dec 2025 with backend/full-stack experience (React/TS, Django/FastAPI/Laravel, PostgreSQL/MongoDB, AWS/Docker/RabbitMQ, JWT/OAuth, GitHub portfolio).

Core functionality:
1. Fetch job postings daily for "software developer intern", "backend developer co-op", "application developer entry-level" in "Canada remote" or "Toronto ON" using free/public APIs (e.g., JobsPikr Indeed scraper, Adzuna, Jooble, or RapidAPI free tiers - avoid paid/restricted Indeed/LinkedIn official APIs; use scraping libraries like Firecrawl/BeautifulSoup if needed with ethical rate limiting).

2. Parse 3-5 user resume files (PDF/DOCX via PyMuPDF/pypandoc) stored in ./resumes/. Extract skills, experience, education matching your profile: Honors BTech Software Development (Seneca, Dec 2025), Migranium backend dev (microservices, APIs, testing 90%+ coverage), OPS I&IT internship (data analysis, UAT, automation).

3. For each job: Use Claude/Groq/GPT API (structured output schema) to score each resume (0-100%) on keyword match (ATS optimization: React, TypeScript, Python/Django/FastAPI, SQL, Docker, AWS, Agile, testing), experience alignment (backend APIs, microservices, cloud), education fit. Rank resumes, explain top match in 2-3 bullet points (e.g., "90% match: Strong Django/FastAPI + RabbitMQ aligns with microservices req; OPS UAT covers testing").

4. Output to Google Sheets (gspread API - include OAuth setup) or local Excel (openpyxl): Columns = Job_Title, Company, Location, URL, Salary_Range, Post_Date, Top_Resume, Match_Score, Reasons (bullets), Apply_Now (Yes/No >80%).

Tech stack (match my skills): FastAPI backend (async), React/Next.js frontend (dashboard: job list, filters, preview matches), PostgreSQL (jobs/resume_scores), Docker compose, Railway deploy. Cron/scheduler for daily runs. Discord webhook for >85% matches.

Structure:
- /scraper: Job fetcher (asyncio, 50 jobs/day limit)
- /parser: Resume/job text extraction + TF-IDF/cosine similarity baseline
- /matcher: LLM prompt for scoring (chain-of-thought: extract JD skills → compare resume → score + reasons)
- /sheets: Auto-append with formatting (color-code scores)
- /ui: React app.py (Streamlit) for manual trigger/view
- config.yaml: API keys, resume paths, search terms

Include:
- Error handling, logging (structlog)
- Rate limiting, ethical scraping notes
- Full README: setup, run, deploy
- Sample data/tests
- Cost estimate (<$5/mo)

Generate ALL code files. Make it deployable today.